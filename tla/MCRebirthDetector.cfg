\* The shipped semantics: the conflicting-identity detector fails
\* divergent-camp merges closed, so an acquired copy's content is stable.
SPECIFICATION Spec
CONSTANTS
    Members = {m1, m2, m3}
    m1 = m1
    m2 = m2
    m3 = m3
    Tags = {t1, t2}
    t1 = t1
    t2 = t2
    DetectorOn = TRUE
    NoValue = NoValue
INVARIANTS
    TypeOK
    TagStability
CHECK_DEADLOCK FALSE
