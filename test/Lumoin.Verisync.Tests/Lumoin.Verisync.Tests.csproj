<Project Sdk="MSTest.Sdk">

  <PropertyGroup>
    <ImplicitUsings>enable</ImplicitUsings>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <EnableTrimAnalyzer>false</EnableTrimAnalyzer>
    <EnableAotAnalyzer>false</EnableAotAnalyzer>
    <!--
      Displays error on console in addition to the log file. Note that this feature comes with a performance impact.
      For more information, visit https://learn.microsoft.com/dotnet/core/testing/unit-testing-platform-integration-dotnet-test#show-failure-per-test
      -->
    <TestingPlatformShowTestsFailure>true</TestingPlatformShowTestsFailure>
    <RunSettingsFilePath>$(MSBuildProjectDirectory)\config.runsettings</RunSettingsFilePath>
    <TestingExtensionsProfile>AllMicrosoft</TestingExtensionsProfile>
    <IsTestApplication>true</IsTestApplication>
    <MSTestAnalysisMode>Recommended</MSTestAnalysisMode>
    <!-- MSTest.Sdk injects its extension references after the project body, so their versions are
         steered through these SDK-recognized properties; PackageReference Update blocks never match
         the injected items. Keep Directory.Packages.props in step — verify-packages.ps1 checks the
         signatures of exactly the versions listed there. -->
    <MicrosoftTestingExtensionsCodeCoverageVersion>18.8.0</MicrosoftTestingExtensionsCodeCoverageVersion>
    <!-- Tests define source-generated JSON contexts and CBOR encoders to exercise the serialization
         adapters, so they opt out of the serialization banlist. -->
    <EnableSerializationBan>false</EnableSerializationBan>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="CsCheck" />
    <PackageReference Include="LiquidTestReports.Markdown" />
    <PackageReference Include="Microsoft.Extensions.Diagnostics.Testing" />
    <PackageReference Include="Microsoft.Extensions.TimeProvider.Testing" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Lumoin.Verisync.Core\Lumoin.Verisync.Core.csproj" />
    <ProjectReference Include="..\..\src\Lumoin.Verisync.Json\Lumoin.Verisync.Json.csproj" />
    <ProjectReference Include="..\..\src\Lumoin.Verisync.Cbor\Lumoin.Verisync.Cbor.csproj" />
  </ItemGroup>

</Project>
